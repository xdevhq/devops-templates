name: Azure Login

inputs:
  azure_credentials:
    required: true

runs:
  using: composite
  steps:
    - name: Login to Azure
      uses: azure/login@v2
      with:
        creds: ${{ inputs.azure_credentials }}
